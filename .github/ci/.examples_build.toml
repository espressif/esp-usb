paths = [
    "examples/peripherals/usb/device",     # USB Device examples
    "examples/peripherals/usb/host",        # USB Host examples
]

recursive = true
#manifest_file = "examples/peripherals/.build-test-rules.yml"        # Extra manifest file from esp-idf
check_warnings = true
target = "all"

# build related options
build_dir = "build_@t_@w"
work_dir = "@f_@t_@w"
